## 1. Specification
- [x] 1.1 Define the split-screen capability with current-display split and merge requirements
- [x] 1.2 Validate the OpenSpec change with strict validation

## 2. Implementation
- [x] 2.1 Add explicit split and merge commands to the manifest
- [x] 2.2 Replace the background split logic with current-display tab-to-window splitting and grid layout placement
- [x] 2.3 Replace the background merge logic with current-display full-window merge behavior
- [x] 2.4 Add split and merge context menu items and click handlers

## 3. Verification
- [ ] 3.1 Manually verify split behavior for active tab and multi-selected tabs
- [ ] 3.2 Manually verify merge behavior only affects windows on the current display
